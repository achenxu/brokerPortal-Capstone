// import { fromJS } from 'immutable';
// import { makeSelectNavigationDomain } from '../selectors';

// const selector = makeSelectNavigationDomain();

describe('makeSelectNavigationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
